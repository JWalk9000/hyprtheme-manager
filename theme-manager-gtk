#!/usr/bin/env python3

import sys
import os
from pathlib import Path

# Add the Theme-Manager directory to Python path
theme_manager_dir = Path(__file__).parent
sys.path.insert(0, str(theme_manager_dir))

# Now we can run the GTK app
from main import main

if __name__ == "__main__":
    main()
